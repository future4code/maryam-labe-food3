import { createTheme } from "@mui/material";

export const theme = createTheme({
  palette: {
    type: 'light',
    primary: {
      main: "#e8222e",
      contrastText: "black",
    },
    secondary: {
      main: "#b8b8b8",
    },
  },
});